import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { HttpClient } from '@angular/common/http';
@Component({selector:'app-pm',standalone:true,imports:[CommonModule],template:`<section class="page"><div class="title"><div><h2>Preventive Maintenance</h2><p>Persisted PM obligations with due / overdue calculation</p></div><button class="btn btn-primary">+ Generate Obligation</button></div><div class="card"><table><tr><th>Vehicle</th><th>Plan</th><th>Trigger</th><th>Remaining</th><th>Status</th></tr><tr *ngFor="let x of rows"><td>{{x.vehicle}}</td><td>{{x.plan}}</td><td>{{x.trigger}}</td><td>{{x.remaining}}</td><td>{{x.status}}</td></tr></table></div></section>`,styles:[`.page{padding:26px}.title{display:flex;justify-content:space-between;align-items:center}.title h2{margin:0}.title p{color:#64748b}.card{margin-top:16px}`]})
export class PmComponent implements OnInit{rows:any[]=[];constructor(private http:HttpClient){}ngOnInit(){this.http.get<any[]>('/api/pm/obligations').subscribe(x=>this.rows=x)}}