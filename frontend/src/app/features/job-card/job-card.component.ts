import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { HttpClient } from '@angular/common/http';
@Component({selector:'app-job-card',standalone:true,imports:[CommonModule,FormsModule],template:`<section class="page"><h2>Job Cards</h2><div class="card"><table><tr><th>Job Card</th><th>Vehicle</th><th>Event</th><th>Bay</th><th>Status</th><th></th></tr><tr *ngFor="let x of cards" (click)="select(x)"><td>{{x.jobCardNumber}}</td><td>{{x.vehicle}}</td><td>{{x.eventNumber}}</td><td>{{x.bay}}</td><td>{{x.status}}</td><td><button class="btn btn-outline">Open</button></td></tr></table></div><div class="card" *ngIf="selected"><h3>{{selected.jobCardNumber}} · Work Items</h3><div class="add"><input [(ngModel)]="description" placeholder="Work item description"><button class="btn btn-primary" (click)="add()">Add Work Item</button></div><table><tr><th>Description</th><th>Type</th><th>Status</th><th></th></tr><tr *ngFor="let w of work"><td>{{w.description}}</td><td>{{w.workType}}</td><td>{{w.status}}</td><td><button class="btn btn-outline" *ngIf="w.status!=='Completed'" (click)="complete(w)">Complete</button></td></tr></table></div></section>`,styles:[`.page{padding:26px}.card{margin-top:16px}.add{display:flex;gap:8px;margin-bottom:10px}.add input{flex:1;padding:9px}`]})
export class JobCardComponent implements OnInit{cards:any[]=[];selected:any;work:any[]=[];description='';constructor(private http:HttpClient){}ngOnInit(){this.http.get<any[]>('/api/job-cards').subscribe(x=>this.cards=x)}select(x:any){this.selected=x;this.http.get<any[]>(`/api/job-cards/${x.id}/work-items`).subscribe(w=>this.work=w)}add(){if(!this.description)return;this.http.post(`/api/job-cards/${this.selected.id}/work-items`,{workType:'Repair',description:this.description,standardRepairHours:1,requiresQc:true,requiresHvAuthorization:false}).subscribe(()=>{this.description='';this.select(this.selected)})}complete(w:any){this.http.put(`/api/work-items/${w.id}/status`,{status:'Completed'}).subscribe(()=>this.select(this.selected))}}
