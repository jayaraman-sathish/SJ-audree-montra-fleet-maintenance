import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { HttpClient } from '@angular/common/http';
@Component({selector:'app-parts',standalone:true,imports:[CommonModule,FormsModule],template:`<section class="page"><h2>Parts & Inventory</h2><p class="muted">Issue / return transactions against job cards</p><div class="card"><div class="fields"><select [(ngModel)]="form.jobCardId"><option value="">Job Card</option><option *ngFor="let j of jobs" [value]="j.id">{{j.jobCardNumber}} · {{j.vehicle}}</option></select><input [(ngModel)]="form.partNumber" placeholder="Part number"><input [(ngModel)]="form.partDescription" placeholder="Description"><select [(ngModel)]="form.transactionType"><option>Issue</option><option>Return</option><option>Reserve</option></select><input type="number" [(ngModel)]="form.quantity" placeholder="Qty"><button class="btn btn-primary" (click)="save()">Post</button></div></div><div class="card"><table><tr><th>Part</th><th>Description</th><th>Type</th><th>Qty</th><th>Date</th></tr><tr *ngFor="let x of rows"><td>{{x.partNumber}}</td><td>{{x.partDescription}}</td><td>{{x.transactionType}}</td><td>{{x.quantity}}</td><td>{{x.transactionAt|date:'short'}}</td></tr></table></div></section>`,styles:[`.page{padding:26px}.muted{color:#64748b}.card{margin-top:16px}.fields{display:grid;grid-template-columns:2fr 1fr 2fr 1fr 1fr auto;gap:8px}.fields>*{padding:8px}`]})
export class PartsComponent implements OnInit{jobs:any[]=[];rows:any[]=[];form:any={jobCardId:'',partNumber:'',partDescription:'',transactionType:'Issue',quantity:1,serialNumber:'',warrantyCandidate:false,failedPartDisposition:'',authorizationStatus:'Not Required'};constructor(private http:HttpClient){}ngOnInit(){this.http.get<any[]>('/api/job-cards').subscribe(x=>this.jobs=x);this.load()}load(){this.http.get<any[]>('/api/parts').subscribe(x=>this.rows=x)}save(){this.form.quantity=Number(this.form.quantity);this.http.post('/api/parts',this.form).subscribe({next:()=>{this.load();this.form.partNumber='';this.form.partDescription=''},error:e=>alert(e.error?.message||'Unable to post')})}}
